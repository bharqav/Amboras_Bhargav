# Nest API base (must be the backend, not the Next app). Used by next.config rewrites to proxy /api/v1/*.
# If unset, next.config reads PORT from ../backend/.env (fallback 4000).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
